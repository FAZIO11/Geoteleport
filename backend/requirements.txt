# Location Spoofer — runtime dependencies
# Tested on macOS 12+ with Python 3.10–3.14.
# All versions pinned for reproducible installs.
#
# Note: every pinned package here ships prebuilt wheels for Python 3.10–3.14,
# so `pip install` will NOT need to compile any Rust or C code.

# iOS communication over USB. Handles lockdownd, DVT, location simulation.
pymobiledevice3==4.14.16

# Local HTTP server.
fastapi==0.136.1
uvicorn==0.46.0

# FastAPI's data-validation core. 2.13.3 ships pydantic-core 2.46.3 which has
# prebuilt wheels for Python 3.14 — older pins (e.g. 2.10.x with core 2.27.x)
# don't, and pip will try to build it from source via Rust.
pydantic==2.13.3

# Pinned to keep the resolver predictable across machines.
starlette==0.49.3
anyio==4.13.0
